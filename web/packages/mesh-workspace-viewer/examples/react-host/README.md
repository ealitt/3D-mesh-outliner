# React Host Example

This folder is a lightweight reference for embedding the package in a host app.

See [`App.tsx`](./App.tsx) for a minimal usage example that wires the exported settings hook, settings button, and viewer component together.
